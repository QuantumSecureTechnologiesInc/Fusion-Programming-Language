[package]
name = "fusion_mobile_ios"
version = "2.0.0"
edition = "2021"
description = "iOS runtime bridge for Fusion v2.0 Vortex (FFI abstractions)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
