[package]
name = "fusion_intent"
version = "0.3.0"
edition = "2021"
license = "MIT OR Apache-2.0"
repository = "https://github.com/QuantumSecureTechnologiesInc/Fusion"
authors = ["Quantum Secure Technologies Inc."]
description = "Intent annotation parsing, task profile generation, and priority-based execution routing"

[dependencies]
parking_lot = "0.12"
tracing = "0.1"
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
