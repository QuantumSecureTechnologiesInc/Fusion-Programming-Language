[package]
name = "fusion_web_runtime"
version = "2.0.0"
edition = "2021"
description = "Browser-based WASM runtime for Fusion v2.0 Vortex"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
